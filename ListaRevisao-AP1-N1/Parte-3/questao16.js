/**
 * Sobre métodos de iteração em arrays: Explique como funcionam os métodos de iteração em arrays em JavaScript: forEach(), map(), filter() e reduce().

R:

    forEach(): Itera sobre os elementos, executando um callback para cada um (geralmente para efeitos colaterais). Retorna undefined.

    map(): Itera e transforma cada elemento, retornando um novo array de mesmo tamanho com os resultados.

    filter(): Itera e seleciona elementos baseados em uma condição, retornando um novo array com os elementos que passaram.

    reduce(): Itera e acumula os valores do array em um único valor (o acumulador).
 */